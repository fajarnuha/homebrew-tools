# Fajar's Homebrew tools

Install Klassify on macOS:

```sh
brew install fajarnuha/tools/klassify
klassify --help
```

The tap can hold other command-line tools without naming it after any one project. The existing [`homebrew-kson`](https://github.com/fajarnuha/homebrew-kson) tap remains available for kson users.
